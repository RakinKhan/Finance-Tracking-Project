import React from "react";
import "./Home.css";

const Home = (props: any) => {
  return (
    <div className={"container"}>
      <div>
        <h1>Down to The Last Dollar</h1>
        <p>
          This app provides convenient way for users to track not only their
          stock investments but also the costs associated with those
          transactions.
        </p>
      </div>
    </div>
  );
};

export default Home;
